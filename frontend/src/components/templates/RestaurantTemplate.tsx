import React from "react";
import { Layout } from "../../layout/Layout";
import { RestaurantDetails } from "../organisms/RestaurantDetails";

export const RestaurantsTemlate = () => {
  return (
    <Layout>
      <RestaurantDetails />
    </Layout>
  );
};
