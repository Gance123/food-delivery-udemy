import React from 'react'
import { Layout } from '../../layout/Layout'
import { LoginFunk } from '../organisms/LoginFunk'

export const LoginTemplate = () => {
  return (
    <Layout>
        <LoginFunk />
    </Layout>
  )
}

